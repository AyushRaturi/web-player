# Use this docker-compose file to run the web music player
version: "3.8"
services:
  web-music-player:
    build:
      context: .
    container_name: web-music-player
    ports:
      - "6969:80"

